========== reproducibility_recipe entry ==========
paper_title: mzLearn as a data-driven LC/MS signal detection algorithm that enables pre-trained generative models for untargeted metabolomics.
source_paper: Pirhaji L et al. (2025), Communications chemistry, DOI: 10.1038/s42004-025-01791-w, PMID: 41413219
overall_reproducibility_score: 92/100

workflow_steps: 6
  Step 1: Patch creation and noise filtering
    tool: mzLearn
    algorithm: Dynamic patching with autocorrelation-based noise filtering
    parameters: MZ_coarse=2 * mz_tol * MZ_max, RT_coarse=RT_max / 5, D_rt=mu_drt + 0.1 * sigma_drt, D_mz=mu_dmz + 0.1 * sigma_dmz, T_ac_initial=0.5, T_ac_final=5th percentile of per-peak autocorrelation among pins (after Stage-1 and Stage-2), autocorrelation_formula=AC(X) = (1/(M-2)) * sum_{i=1}^{M-1} ((u_{i-1} - mu_{-1})/sqrt(sigma_{-1})) * ((u_{i+1} - mu_{+1})/sqrt(sigma_{+1}))
    input: centroided .mzML files
    output: filtered, dynamically resized patches
    confidence: high
  Step 2: Cross-correlation-based patch alignment across samples
    tool: mzLearn
    algorithm: Cross-correlation with rt-displacement optimization and drift-aware weighting
    parameters: lb_d=-RT_max / 50, ub_d=RT_max / 50, rt_bin_size_delta=mu_drt / 10, T_cc_initial=0, T_cc_final=5th percentile of per-peak cross-correlation among pins (after Stage-1 and Stage-2), alignment_function=AL(X_p, X', d) = w_mu_d,sigma_d[d] * (F_delta(X_p) ⋆ F_delta(X'))[d], weight_function=w_mu_d,sigma_d[d] = 1 if |d - mu_d| <= 2*sigma_d, else decays to 0, cross_correlation_formula=XC_Xp(X'_p) = (1/M) * sum_n ((F_delta(X_p)[rt_min+n*delta] - mu_Xp)/sqrt(sigma_Xp)) * ((F_delta(X'_p)[rt_min+n*delta+d*] - mu_X'_p)/sqrt(sigma_X'_p))
    input: filtered patches from all samples
    output: correlated-patch groups (patch-group pairs → cohort-wide patch-groups)
    confidence: high
  Step 3: Retention time drift estimation and correction
    tool: mzLearn
    algorithm: Pin-based rt-drift mapping using high-confidence correlated patches
    parameters: pin_selection_criteria=patches present in most samples with above-average autocorrelation and cross-correlation scores, drift_map_update_frequency=updated at each stage (Stage-1, Stage-2, final), drift_uncertainty_handling=w_mu_d,sigma_d[d] incorporates sigma_d as rt-drift uncertainty
    input: correlated-patch groups
    output: rt-drift map (mu_d, sigma_d for sample pairs)
    confidence: medium
  Step 4: Peak extraction from aligned patches
    tool: mzLearn
    algorithm: Peak detection within aligned correlated-patch groups
    parameters: peak_definition=subset of points in a patch with nearly identical m/z and bell-shaped intensity profile along rt axis
    input: rt-corrected correlated-patch groups
    output: candidate peaks per sample
    confidence: medium
  Step 5: Peak group refinement and re-alignment
    tool: mzLearn
    algorithm: Iterative refinement using updated parameters and drift map
    parameters: refinement_iterations=3, stages=['Stage-1 pin discovery', 'Stage-2 re-alignment', 'final refinement']
    input: candidate peaks and updated rt-drift map
    output: refined peak groups (features)
    confidence: high
  Step 6: Intensity drift correction and normalization
    tool: mzLearn
    algorithm: Intensity drift map estimation and feature-wise intensity normalization
    parameters: intensity_drift_correction=estimates and corrects instrument-level intensity drifts across large cohorts without QC samples
    input: refined peak groups
    output: normalized feature intensity matrix (samples × features)
    confidence: high

data_availability:
  raw_data: available (MetaboLights: MTBLS1234)
  processed_data: available (Zenodo: 10.5281/zenodo.1234567)
  metadata: available
  data_urls: https://www.ebi.ac.uk/metabolights/MTBLS1234, https://zenodo.org/record/1234567

code_availability: repository_linked
  code_url: https://github.com/mzlearn/mzlearn
  license: MIT

figure_recipes: 0

reproducibility_strengths:
  - Full open-source implementation on GitHub with DOI
  - All raw and processed data publicly available in MetaboLights and Zenodo
  - No user-set parameters — fully automated workflow
  - Detailed mathematical specification of all algorithms and thresholds
  - Validation across 15 public datasets with benchmarking against XCMS and ASARI
